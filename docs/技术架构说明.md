# 技术架构说明

本文档说明 赛马娘猜猜乐的模块组成、数据流、部署结构和安全边界。

## 一、总体架构

<p align="center">
  <img src="./assets/架构总览.svg" alt="架构总览">
</p>

项目采用单仓库一体化结构：

- 页面、接口与服务逻辑都在 `Next.js` 项目内
- 数据通过仓库内标准化文件生成，并同步导入数据库
- 图片通过项目自己的资源路径交付
- 多人、好友房和观战能力沿用同一套身份、房间和同步基础设施

## 二、模块分层

### 1. 表现层

目录：

- `src/app/`
- `src/components/`

职责：

- 首页、单人页、匹配页、好友房页面渲染
- 搜索输入、结果表、卡片布局、状态弹窗
- 移动端与桌面端布局适配

### 2. 接口层

目录：

- `src/app/api/`

职责：

- 创建单人对局
- 提交猜测
- 读取对局状态
- 创建 / 加入 / 同步房间
- 资源代理与图片返回

### 3. 业务层

目录：

- `src/lib/game/`
- `src/lib/battle/`
- `src/lib/auth/`

职责：

- 字段比较与结果构建
- 房间逻辑、对战逻辑、暂停 / 投降 / 再来一局
- 浏览器会话标识、只读分享、房主权限识别

### 4. 配置层

目录：

- `src/config/`

职责：

- 统一读取环境变量
- 区分公开配置与服务端私密配置
- 校验配置合法性

### 5. 数据层

目录：

- `data/`
- `src/lib/db/`

职责：

- 保存标准化角色数据与题库产物
- 保存手工维护字段
- 通过 Drizzle 操作 PostgreSQL

## 三、数据流

### 1. 题库数据流

```text
data/normalized/characters
  -> scripts/build-question-bank.ts
  -> data/question_bank_ready.json
  -> scripts/build-search-index.ts
  -> public/search/characters.json
  -> scripts/seed-question-bank.ts
  -> PostgreSQL
```

### 2. 游戏状态数据流

```text
前端请求
  -> API Route
  -> 业务服务
  -> PostgreSQL / 本地回退存储
  -> 返回可展示状态
```

### 3. 图片资源数据流

```text
前端资源请求
  -> 项目资源路径
  -> 本地缓存命中则直接返回
  -> 缓存缺失时受控回源
  -> 写入缓存与素材清单
  -> 后续优先复用缓存
```

## 四、技术选型

### 应用框架

- `Next.js 16`
- `React 19`
- `TypeScript`

选择原因：

- 便于同时维护页面与接口
- 适合 SEO、分享链接、路由管理和渐进式交付
- 统一类型系统，便于多人协作

### 样式与交互

- `Tailwind CSS`
- `Motion`
- `TanStack Table`

选择原因：

- 方便快速调整 UI 节奏与响应式布局
- 适合构建桌面和移动端两套展示结构
- 结果表与卡片布局容易扩展

### 数据与缓存

- `PostgreSQL`
- `Redis`
- `Drizzle ORM`

选择原因：

- 关系型数据适合对局、记录、资源台账
- Redis 适合限流、同步、状态辅助
- Drizzle 与 TypeScript 结合紧密，类型清晰

## 五、安全边界

### 1. 人机验证

- 使用 `Cloudflare Turnstile`
- 入口创建对局或加入流程会按配置启用校验

### 2. 限流

- 默认优先使用 Redis
- 在非生产环境或特殊回退场景下可退回内存限流

### 3. 会话与只读分享

- 使用临时会话标识区分可操作用户与只读访问者
- 分享链接默认只提供查看能力，不提供落猜权限

### 4. 答案保护

- 对局进行中刷新状态不直接暴露答案
- 只有结束态或明确允许的读取路径才会返回答案

## 六、部署结构

### Docker 结构

- `app`
- `app-init`
- `postgres`
- `redis`

### 非 Docker 结构

- `Next.js` 服务进程
- `PM2` 或系统服务
- `PostgreSQL`
- `Redis`
- `Nginx` 或面板反向代理

## 七、维护重点

- 玩法字段改动时，同步更新数据构建与前端展示
- 配置项改动时，同步更新 `.env.example`
- 图片来源或缓存策略变更时，同步更新素材台账与文档
- 新增模式时，尽量复用现有会话、房间和同步能力

